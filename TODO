libp000.4gl:fl_mayorizacion_mes()
FIXME:
- En algun lugar se esta incluyendo la cuenta 62010102, en el caso de equirent esa cuenta no existe y estaba dando un error al remayorizar.
  Se solucionó creando la cuenta en la ctbt010, pero aun queda la duda de porque procesa una cuenta no existente.

- Crear un programa de configuracion del fobos, debe tener una pantalla para
  la tabla fobos, cargar datos bàsicos (pais, ciudad, etc) y llamar a la
  genp100 y genp101 (se puede llamar a esas pantallas sin que haya
  contabilidad?)

- Hacer que los reportes salgan en el $HOME del usuario en lugar de una
  carpeta que todo el mundo pueda ver
